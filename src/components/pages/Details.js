import { VscChevronLeft } from 'react-icons/vsc';

const Details = () => (
  <section>
    <button type="button">
      <VscChevronLeft style={{ fontSize: '30px' }} />
    </button>
  </section>
);

export default Details;
